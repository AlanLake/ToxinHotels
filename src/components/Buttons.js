import React from "react";

export const ButtonPurple = (props) => {
  return (
    <>
      <button className="button_purple">{props.name}</button>
    </>
  );
};

export const ButtonPurpleFaded = (props) => {
  return (
    <>
      <button className="button_purple_faded">{props.name}</button>
    </>
  );
};

export const BlankButton = (props) => {
  return (
    <>
      <button className="button_blank">{props.name}</button>
    </>
  );
};

export const BlankButtonFaded = (props) => {
  return (
    <>
      <button className="button_blank_faded">{props.name}</button>
    </>
  );
};

export const WideButton = (props) => {
  return (
    <>
      <button className="button_wide">{props.name}</button>
    </>
  );
};

export const BorderlessButton = (props) => {
  return (
    <>
      <button className="button_borderless">{props.name}</button>
    </>
  );
};

export const BorderlessButtonFaded = (props) => {
  return (
    <>
      <button className="button_borderless_faded">{props.name}</button>
    </>
  );
};


